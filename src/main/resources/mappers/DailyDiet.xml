<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dailyDiet">
  <select id="selectAllByStartDate" parameterType="String" resultType="DailyDiet">
    SELECT *
    FROM daily_diet
    WHERE start_date BETWEEN TO_DATE(#{startDateFormat}, 'YYYY-MM-DD')
            AND (TO_DATE(#{startDateFormat}, 'YYYY-MM-DD') + INTERVAL '13' DAY)
  </select>
</mapper>
